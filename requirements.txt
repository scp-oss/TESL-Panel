Flask>=3.0
requests>=2.31
gunicorn>=22.0
# Запасной sqlite3 для builds_db.py — самодостаточная сборка (свой
# статически слинкованный libsqlite3 внутри wheel), нужна только если
# у системного Python нет собственного модуля _sqlite3 (см.
# panel/builds_db.py, живой инцидент 2026-09-23). Держим её здесь
# безусловно, а не под каким-то условным маркером — pip не умеет
# ставить пакет "если такой-то stdlib-модуль отсутствует", а сама
# установка безвредна и на нормальном Python: builds_db.py сначала
# пробует stdlib `sqlite3` и берёт это только как fallback.
pysqlite3-binary>=0.5
